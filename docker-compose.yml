version: '3.8'

services:
  backend:
    build: .
    container_name: song-library-backend
    ports:
      - '3000:3000'
    depends_on:
      - postgresDB
    env_file:
      - ./.env
    environment:
      - POSTGRES_URL=postgresql://postgres:example@postgresDB:5432/songsdb

  postgresDB:
    image: postgres:latest
    container_name: postgresDB
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=example
      - POSTGRES_DB=songsdb
  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    ports:
      - '8080:80'
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    depends_on:
      - postgresDB
volumes:
  postgres_data:
